name: Build (CI)

permissions:
  contents: read

on:
  push:
    branches: [dev]
    paths-ignore: &ignored
      - '**/.editorconfig'
      - '**/.gitignore'
      - '**/.gitattributes'
      - '**/*.md'
      - '**/LICENSE'
      - '**/LICENCE'
      - 'PCL.Core.Test/**'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/FUNDING.yml'
  pull_request:
    paths-ignore: *ignored
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - configuration: CI
            architecture: x64
            project_file: Plain Craft Launcher 2/Plain Craft Launcher 2.csproj
            artifact_name_suffix: _CSharp
          - configuration: CI
            architecture: ARM64
            project_file: Plain Craft Launcher 2/Plain Craft Launcher 2.csproj
            artifact_name_suffix: _CSharp
          - configuration: CI
            architecture: x64
            project_file: Plain Craft Launcher 2/Plain Craft Launcher 2.vbproj
            artifact_name_suffix: _VB
          - configuration: CI
            architecture: ARM64
            project_file: Plain Craft Launcher 2/Plain Craft Launcher 2.vbproj
            artifact_name_suffix: _VB
      fail-fast: false

    uses: ./.github/workflows/reusable-build.yml
    with:
      configuration: ${{ matrix.configuration }}
      architecture: ${{ matrix.architecture }}
      project_file: ${{ matrix.project_file }}
      artifact_name_suffix: ${{ matrix.artifact_name_suffix }}
    secrets: inherit
