# PCL Online Staging

This directory temporarily holds Online UI surfaces that were removed from the
desktop application package. They are intentionally not referenced by the main
solutions or desktop project.

Future Online functionality should be provided by the plugin SDK and registered
through host module/page injection instead of being compiled into `PCL.Desktop`.
